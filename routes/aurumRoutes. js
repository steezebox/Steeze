const express = require('express');
const router = express.Router();
const { buyAurum } = require('../controllers/aurumController');

router.post('/buy', buyAurum);

module.exports = router;
const { spendAurum } = require('../controllers/aurumController');
router.post('/spend', spendAurum);